1) Copy a zipped and GPG encrypted export to this directory
2) Run ./decrypt.sh $filename
  Where $filename is the filename of the encrypted export

Now unzip the decrypted data

3) cd ./output
4) unzip decrypted_data


