
= content_for :title do
  #{_(@record['systemid'])}

= content_for :content do
  #record.grid_10
    = @record['systemid']
  asdf

= content_for :navigation do
  = render_menu(1..2, record_navigation(@record))



= render :template => "layouts/tupane_layout"
